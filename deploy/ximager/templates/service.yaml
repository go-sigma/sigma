apiVersion: v1
kind: Service
metadata:
  name: {{ include "ximager.fullname" . }}
  labels:
    {{- include "ximager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ximager.selectorLabels" . | nindent 4 }}
