linters:
  enable:
    - cyclop
    - exportloopref
    - gocritic
    - gosec
    - ineffassign
    - misspell
    - prealloc
    - unconvert
    - unparam
    - goimports
    - whitespace
    - gci

linters-settings:
  whitespace:
    multi-func: true
  cyclop:
    max-complexity: 30
    package-average: 15
    skip-tests: true
  gci:
    sections:
      - standard
      - blank
      - default
      - prefix(github.com/ximager/ximager)
      - dot
